server:
  port: 7089

spring:
  profiles:
    active: dev
  application:
    name: nacos
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  config: classpath:log/logback-spring.xml
