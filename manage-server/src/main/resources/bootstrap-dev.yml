spring:
  cloud:
    nacos:
      config:
        # 服务器地址
        server-addr: 39.105.132.152:8848
        # 命名空间
        namespace: dev
        # Group
        group: DEFAULT_GROUP
        # Data Id 前缀
        prefix: ${spring.application.name}
        # Data Id 后缀
        file-extension: yaml
      discovery:
        # 服务器地址
        server-addr: 39.105.132.152:8848
        # 命名空间
        namespace: dev
        metadata:
          version: v1